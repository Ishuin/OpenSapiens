package org.opensapien.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette for open_sapien. See `design-system/MASTER.md`.
 *
 * Two accents with strictly separate jobs:
 *  - [EmberDark] / [EmberLight] — armed, selected, focused. The record lamp on a tape deck.
 *  - [SignalDark] / [SignalLight] — capturing right now, and destructive actions.
 * They are never used interchangeably.
 */

// Dark theme ---------------------------------------------------------------
val BackgroundDark = Color(0xFF0B0B0C)
val SurfaceDark = Color(0xFF141416)
val SurfaceVariantDark = Color(0xFF1D1D20)
val OutlineDark = Color(0xFF2C2C31)
val OnBackgroundDark = Color(0xFFF2F2EF)
val OnSurfaceVariantDark = Color(0xFF9A9AA2)
val EmberDark = Color(0xFFE8853A)
val SignalDark = Color(0xFFE0483C)
val SuccessDark = Color(0xFF4E9A6A)

// Light theme --------------------------------------------------------------
val BackgroundLight = Color(0xFFF7F7F5)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFECECE8)
val OutlineLight = Color(0xFFD6D6D0)
val OnBackgroundLight = Color(0xFF17171A)
val OnSurfaceVariantLight = Color(0xFF5C5C63)
val EmberLight = Color(0xFFB4551A)
val SignalLight = Color(0xFFC2352A)
val SuccessLight = Color(0xFF2F6B45)
